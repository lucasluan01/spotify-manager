import { TrackModel } from "./track.model";

export interface TrackListModel {
    // href: string
    items: TrackModel[]
    // limit: number
    // next: any
    // offset: number
    // previous: string
    // total: number
}